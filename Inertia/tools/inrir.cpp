/* THE CLI TOOL FOR COMPILING IR */
// This tool compiles the IR (.inr) files into outputs such as:
// Object files (.o), assembly (.s), or executables.
// Files above are examples, it is not limited to only those above.
// This is needed when the IR comes from a file, not the frontend.

int main(int argc, char** argv){
    if(argc == 1){
        // no inputs provided
    }

    return 0;
}