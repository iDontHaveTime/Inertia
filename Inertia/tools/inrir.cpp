/* ==================================================== */
// THE CLI TOOL FOR COMPILING IR
// This tool compiles the IR (.inr) files into either object files (.o) or Assembly (.s).
// This is needed when the IR comes from a file, not the frontend.
/* ==================================================== */

int main(int argc, char** argv){
    if(argc == 1){
        // no inputs provided
    }

    return 0;
}
