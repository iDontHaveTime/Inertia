# lib

## Purpose

Contains the C++ implementations for headers.

## Exceptions

Inertia should be compiled with `-fno-exceptions` so functions are marked with noexcept even when they involve stuff like `new`.